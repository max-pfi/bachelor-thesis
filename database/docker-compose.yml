version: '3.8'

services:
  postgres:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${PG_USER}
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_DB: ${PG_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data/01-init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./data/02-replication.sql:/docker-entrypoint-initdb.d/02-replication.sql
      - ./data/03-trigger.sql:/docker-entrypoint-initdb.d/03-trigger.sql
    command: >
      postgres
      -c wal_level=logical
      -c max_wal_senders=10
      -c max_replication_slots=10
      -c max_connections=100
      -c wal_sender_timeout=60s

volumes:
  postgres_data: